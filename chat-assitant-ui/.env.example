# Vercel AI Gateway key. Optional.
# Without it the chat still runs: /api/chat streams a canned offline reply and
# every creative-library command stays fully functional (they are mocked locally).
AI_GATEWAY_API_KEY=
